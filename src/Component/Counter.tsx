import React from 'react';

const Counter = () => {
    return (
        <div>
            <h1>Count: 0</h1>
            <button>Increase</button>
            <button>Reset</button>
            <button>Decrease</button>
        </div>
    );
};

export default Counter;